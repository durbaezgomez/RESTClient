# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RESTClientApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RESTClientApp
  
  pod 'Resolver'
end

def testing_pods
  pod 'Quick'
  pod 'Nimble'
end

target 'RESTClientAppUnitTests' do
  testing_pods
end
